[pytest]
testpaths = tests
addopts = -p no:cacheprovider --ignore=tests/_tmp_pytest_run_01
norecursedirs = .tmp _tmp_pytest_run_*
markers =
    real_gemini_e2e: Uses real Gemini API calls (free-tier-safe pacing enabled in tests).
